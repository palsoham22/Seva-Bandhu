# Seva Bandhu Frontend

This folder contains the current Django-rendered frontend:

- `templates/` — HTML templates for customer and technician flows
- `static/` — public frontend assets such as CSS, JavaScript, and images

The Django backend renders these templates from `backend/seva_bandhu/settings.py`. No standalone frontend framework has been introduced.
